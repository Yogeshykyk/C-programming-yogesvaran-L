#include<stdio.h>
int main()
{
    int a;
    printf("enter the number:");
    scanf("%d",&a);
    if(a%2==0)
    {
        printf(" \ngiven %d is even number",a);
    }
    else
    {
        printf(" \ngiven %d is odd number",a);
}
}